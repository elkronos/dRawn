% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plan.R
\name{sample_summary}
\alias{sample_summary}
\title{Describe a drawn sample}
\usage{
sample_summary(sample)
}
\arguments{
\item{sample}{A data frame returned by \code{\link[=draw]{draw()}} with \code{weights = TRUE}.}
}
\value{
A list with a \code{print()} method, holding:
\describe{
\item{\code{design}}{The design's type, as a string.}
\item{\code{n}, \code{N}}{Rows drawn, and rows in the frame.}
\item{\code{weight_range}}{The smallest and largest design weight.}
\item{\code{weight_cv}}{Their coefficient of variation. Large values mean a
few rows carry most of the estimate.}
\item{\code{unreachable}}{Frame rows with inclusion probability 0 — the
design could never have selected them. \code{NA} if the design has no
closed-form inclusion probability.}
\item{\code{by_group}}{A data frame of \code{group}, \code{drawn}, \code{in_frame} and
\code{rate} per stratum or cluster, or \code{NULL} for a design with no
grouping.}
\item{\code{group_col}}{The column(s) \code{by_group} is keyed on.}
}
}
\description{
What you actually got, against what the design asked for. Worth a look before
analysing: it surfaces strata that came up short, weights that vary more than
you expected, and rows the design could never have reached.
}
\examples{
set.seed(1)
pop <- data.frame(
  id = 1:400,
  site = rep(c("a", "b", "c", "d"), times = c(200, 100, 60, 40))
)
s <- draw(pop, design_stratified("site", n = 40), seed = 1, weights = TRUE)
sample_summary(s)

}
\seealso{
\code{\link[=draw]{draw()}}, \code{\link[=deff]{deff()}}
}
