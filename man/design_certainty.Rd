% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/design-certainty.R
\name{design_certainty}
\alias{design_certainty}
\title{Take some rows with certainty, sample the rest}
\usage{
design_certainty(above, threshold, rest, na_rm = FALSE)
}
\arguments{
\item{above}{Column holding the size measure.}

\item{threshold}{Rows with \code{above >= threshold} are taken with certainty.}

\item{rest}{A design applied to the rows below the threshold. Its \code{n} is the
number drawn from \emph{those} rows, not the total.}

\item{na_rm}{Drop rows whose size measure is \code{NA} instead of raising an
error.}
}
\value{
A design object, for use with \code{\link[=draw]{draw()}}.
}
\description{
Composes a design out of two parts: rows at or above a threshold are all
taken, and any other design is applied to what remains. This is the standard
shape of an audit or financial sample — every invoice over a hundred
thousand gets examined, the long tail below is sampled — and it is the
cheapest way to cut variance when a few units dominate the total.
}
\details{
Certainty rows have an inclusion probability of exactly 1, so they contribute
their own value to a Horvitz-Thompson total with a weight of 1 and add
nothing to its variance. That is the whole point: the uncertainty in the
estimate comes only from the part you sampled.
}
\examples{
set.seed(1)
invoices <- data.frame(
  id = 1:500,
  site = rep(c("a", "b"), times = c(300, 200)),
  value = round(stats::rlnorm(500, 7, 1.4))
)

# Everything over 20,000 examined; 40 drawn from the rest
d <- design_certainty("value", threshold = 20000,
                      rest = design_stratified("site", n = 40))
s <- draw(invoices, d, seed = 1, weights = TRUE)

table(certain = s$.prob == 1)
sum(invoices$value >= 20000)

}
\seealso{
\code{\link[=draw]{draw()}}, \code{\link[=design_weighted]{design_weighted()}} for probability-proportional-to-size,
which handles dominant units by taking them with certainty automatically.

Other designs: 
\code{\link{design_bootstrap}()},
\code{\link{design_cluster}()},
\code{\link{design_multistage}()},
\code{\link{design_reservoir}()},
\code{\link{design_simple}()},
\code{\link{design_spatial}()},
\code{\link{design_stratified}()},
\code{\link{design_systematic}()},
\code{\link{design_temporal}()},
\code{\link{design_weighted}()}
}
\concept{designs}
